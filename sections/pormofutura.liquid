{% assign sec_id = section.id | slice: -8, 8  | replace: "-", "_" %}

{%- style -%}

.pw-section__{{sec_id}} {
  --pw-gutter-x: 25px;
  background-color: {{section.settings.background_color}};
  color: {{section.settings.text_color}};
  {% if section.settings.section_height == true %}
    min-height: 100vh;
  {% endif %}
  padding-top: {{section.settings.section_padding_top}}px;
  padding-bottom: {{section.settings.section_padding_bottom}}px;
  display: flex;
  align-items: center;
  text-align: center;
  line-height: 1.5;
}

.pw-section__{{sec_id}} .pw-section-inner {
  position: relative;
  max-width: 825px;
  margin-left: auto;
  margin-right: auto;
}

/* ✅ Application de la police Futura et suppression du gras */
.pw-section__{{sec_id}} .pw-section-title,
.pw-section__{{sec_id}} p {
  font-family: 'Futura', Arial, sans-serif !important;
  font-weight: normal !important;
  font-size: 24px;
  line-height: 1.4;
}

@media only screen and (max-width: 749px) {
  .pw-section__{{sec_id}} {
    min-height: auto;
    padding-top: {{section.settings.section_padding_top_mobile}}px;
    padding-bottom: {{section.settings.section_padding_bottom_mobile}}px;
  }
  .pw-section__{{sec_id}} .pw-section-title {
    font-size: 20px;
  }
}

{%- endstyle -%}

<div class="pw-section__{{sec_id}}">
  <div class="pw-container">
    <div class="pw-section-inner">
      {% if section.settings.heading_text != blank %}
        <{{ section.settings.heading_tag }} class="pw-section-title">
        {{ section.settings.heading_text | escape }}
        </{{ section.settings.heading_tag }}>
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "🚀SP - Promo-Nova-01",
  "tag": "section",
  "class": "kava-promo kava-promo-nova-01",
  "settings": [
    {
      "type": "header",
      "content": "Main settings"
    }, 
    {
      "type": "textarea",
      "id": "heading_text",
      "default": "Votre texte inspirant ici",
      "label": "Texte principal"
    },
    {
      "type": "select",
      "id": "heading_tag",
      "label": "Balise du titre",
      "options": [
        {
          "value": "h1",
          "label": "H1"
        }, {
          "value": "h2",
          "label": "H2"
        }, {
          "value": "h3",
          "label": "H3"
        }
      ],
      "default": "h2"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Couleur du texte",
      "default": "#262424"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Couleur du fond",
      "default": "transparent"
    },
    {
      "type": "range",
      "id": "section_padding_top",
      "min": 0,
      "max": 300,
      "step": 10,
      "unit": "px",
      "label": "Padding haut",
      "default": 100
    },
    {
      "type": "range",
      "id": "section_padding_bottom",
      "min": 0,
      "max": 300,
      "step": 10,
      "unit": "px",
      "label": "Padding bas",
      "default": 100
    }
  ],
  "presets": [
    {
      "name": "🚀SP - Promo-Nova-01",
      "settings": {
        "heading_text": "Votre message inspirant ici"
      }
    }
  ]
}
{% endschema %}
